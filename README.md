run with `python3 index.py`

A script that pulls data from r/wsb to see which stonks are the most hyped! Seems to provide a pretty decent indicator of which options are going to be pricey if you're interested in selling covered calls.
